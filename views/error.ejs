<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - Easy Agrotech</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Chick Animation */
    .chick {
      animation: bounce 1.5s infinite;
      position: relative;
    }

    @keyframes bounce {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-12px);
      }
    }

    .blink {
      animation: blink 3s infinite;
    }

    @keyframes blink {
      0%, 90%, 100% {
        opacity: 1;
      }
      95% {
        opacity: 0;
      }
    }

    /* Wing flapping */
    .wing {
      position: absolute;
      top: 8px;
      width: 12px;
      height: 12px;
      background-color: #FFD54F;
      border-radius: 50%;
      animation: flap 1s infinite alternate;
    }

    .wing-left {
      left: -6px;
      transform-origin: right center;
    }

    .wing-right {
      right: -6px;
      transform-origin: left center;
    }

    @keyframes flap {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(30deg); }
    }

    /* Feather Glow Effect */
    .feather {
      position: absolute;
      width: 6px;
      height: 6px;
      background: #FFE082;
      border-radius: 50%;
      opacity: 0.8;
      animation: float 2s infinite alternate;
    }

    @keyframes float {
      0% { transform: translateY(0); }
      100% { transform: translateY(-10px); }
    }

  </style>
</head>

<body class="bg-gradient-to-b from-yellow-50 to-yellow-100 flex items-center justify-center min-h-screen p-6">

  <div class="bg-white shadow-2xl rounded-3xl p-8 max-w-xl w-full text-center border-4 border-yellow-400 relative overflow-hidden">
    
    <!-- Feather floating effects -->
    <div class="feather" style="top:10%; left:20%; animation-delay: 0s;"></div>
    <div class="feather" style="top:30%; left:80%; animation-delay: 0.5s;"></div>
    <div class="feather" style="top:70%; left:40%; animation-delay: 1s;"></div>

    <!-- Animated Chick -->
    <div class="flex justify-center mb-6 relative">
      <div class="chick">
        <div class="w-24 h-24 bg-yellow-300 rounded-full border-4 border-yellow-500 relative">
          <!-- Wings -->
          <div class="wing wing-left"></div>
          <div class="wing wing-right"></div>
        </div>
        <!-- Eyes -->
        <div class="absolute top-6 left-5 w-3 h-3 bg-black rounded-full blink"></div>
        <div class="absolute top-6 right-5 w-3 h-3 bg-black rounded-full blink"></div>
        <!-- Beak -->
        <div class="absolute top-10 left-1/2 -translate-x-1/2 w-6 h-4 bg-orange-500 rounded-b-full"></div>
        <!-- Legs -->
        <div class="absolute -bottom-4 left-7 w-2 h-6 bg-orange-600 rounded-full"></div>
        <div class="absolute -bottom-4 right-7 w-2 h-6 bg-orange-600 rounded-full"></div>
      </div>
    </div>

    <!-- Company Name -->
    <h1 class="text-4xl font-extrabold text-green-700 flex justify-center items-center gap-2 animate-pulse">🐥 Easy Agrotech</h1>
    <p class="text-lg text-gray-600 mt-2">Oops! Something went wrong.</p>

    <!-- Error Message -->
    <h2 class="text-2xl font-bold text-red-600 mt-4 animate-bounce"><%= message %></h2>
    <p class="text-md text-gray-500 mt-1">Status: <%= error.status %></p>

    <!-- Go Back Button -->
    <a href="/app_data"
      class="mt-6 inline-block bg-gradient-to-r from-yellow-400 to-yellow-500 hover:from-yellow-500 hover:to-yellow-600 text-black font-semibold px-6 py-3 rounded-full shadow-lg transition transform hover:-translate-y-1">
      🏡 Back to Home
    </a>
  </div>

</body>
</html>
